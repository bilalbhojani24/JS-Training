/**
 * primitive -> integer, string, boolean
 *
 * non primitive -> array, objects
 */

// primitive
let number = 100; // number
const str = "I am a new string!!"; // string
const isAdmin = false; // boolean

// non-primitive
const arr = [10, 20, "Bilal", "Rajesh"];

const customObj = {
  key1: 100, // property [key : value]
  key2: 200,
};

// JS is a loosely coupled bound language

// assingment - Learn about loosely couple and tightly couple language
